# Preserve Kaggle's CUDA-enabled torch/torchvision pair; do not reinstall CUDA here.
# API-compatible pins; real Kaggle GPU acceptance must still run in the notebook.
transformers==4.51.3
peft==0.15.2
accelerate==1.6.0
bitsandbytes==0.45.5
pydantic==2.10.4
nibabel==5.3.2
PyYAML==6.0.2
nbformat==5.10.4
pytest==8.3.5
jsonschema==4.24.0
lm-format-enforcer==0.10.11
# Existing Kaggle pandas/numpy/Pillow/scipy are used and recorded in the receipt.
