# Actual private dataset paths and versions are supplied by the notebook form.
annotations_root: null
images_root: null
images_dataset: null             # owner/slug/version
annotations_dataset: null        # owner/slug/version
work_root: /kaggle/working
cache_root: /tmp/mri-cache
run_name: v1_fold1_smoke_01
task: v1-train
fold: 1
mode: smoke
gpu_index: "0"
max_runtime_minutes: 60
resume_from: null
v1_config: configs/v1_config.yaml
v2_config: configs/v2_report.yaml
